import React from "react"

const Submit = () => {
    return (
    <input
        id="new-submission"
        onChange={() => null}
        name="submit"
        type="submit"
        value="Submit"
        className="pink-button form-button"
      />
    );
}

export default Submit